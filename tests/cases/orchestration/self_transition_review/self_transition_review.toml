[[multi.steps]]
name = "init"
command = "init"
stdin = "y"

[[multi.steps]]
name = "worker"
command = "worker"

[[multi.steps.messages]]
content = ""
label = "main_head_sha"
mode = "exit"

[files]
".claude/settings.json" = '{"permissions":{"allow":["Bash(ls:*)","Bash(cat:*)"]}}'
"src/intervals.py" = ""
"brief.md" = """# Brief

- Add a `merge_intervals` function to `src/intervals.py`. It takes a list of `[start, end]` intervals (list of two-element lists), merges overlapping or adjacent intervals, and returns a sorted list of merged intervals. Handle empty input.
"""
