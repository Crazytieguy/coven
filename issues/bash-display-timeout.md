---
priority: P2
state: approved
---

# Bash tool display should show timeout parameter

When rendering Bash tool calls (both streaming and message viewing with `:N`), the `timeout` parameter is not displayed. It should be shown alongside the command.
